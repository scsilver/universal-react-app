import actionTypes from "../../constants/actionTypes";
const initialState = {
  spinner: false
};
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
