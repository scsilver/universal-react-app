import actionTypes from "../../../constants/actionTypes";
const initialState = {
  expanded: false
};
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
c;
