import actionTypes from "../../../constants/actionTypes";
const initialState = {
  expanded: true
};
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
