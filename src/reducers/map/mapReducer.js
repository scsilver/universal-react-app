import actionTypes from "../../constants/actionTypes";
const initialState = {
  center: { lat: 0, lng: 0 }
};
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
